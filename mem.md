
watcher.py :
  the old approach, work with ~/bin/mywarp.sh
  takes a user question and terminal context as arguments,
  sends them to a local Ollama LLM (gemma3), and prints a concise
  technical answer or fix command. It’s designed to help debug
  terminal issues using AI.
